# Shared Components

Reusable UI component objects belong in this folder. Keep page-specific navigation and assertions in `src/pages/`; place components used by more than one page here, such as `Navbar`, `CartModal`, and `Footer`.
